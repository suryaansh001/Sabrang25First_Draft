# Frontend Environment Variables for Fallback Payment System

# Backend API URL
NEXT_PUBLIC_BACKEND_API_URL=https://your-backend.railway.app

# Cashfree Credentials (PRODUCTION)
# ⚠️ WARNING: These will be exposed in the browser
# Only enable fallback during critical events or when necessary
NEXT_PUBLIC_CASHFREE_CLIENT_ID=your_production_client_id_here
NEXT_PUBLIC_CASHFREE_CLIENT_SECRET=your_production_client_secret_here

# How to use:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Cashfree production credentials
# 3. Restart the Next.js development server
# 4. Check browser console to verify "FALLBACK: ENABLED"

# Security Notes:
# - These credentials will be visible in the compiled JavaScript
# - Anyone can extract them from the browser
# - Consider using separate credentials with lower limits for fallback
# - Set up rate limiting on Cashfree dashboard
# - Monitor for unusual activity
# - Disable fallback when not needed by removing these variables
